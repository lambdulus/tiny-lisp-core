

export class SECDArray extends Array< number | string | boolean| SECDArray | undefined>{
/*
    shift(): number | string | boolean | SECDArray{
        return super.shift() as number | string | boolean | SECDArray;
    }

    pop(): number | string | boolean | SECDArray{
        return super.pop() as number | string | boolean | SECDArray;
    }

    concat(...items): (number | string | boolean | SECDArray)[] {
        return super.concat(items) as (number | string | boolean | SECDArray)[];
    }*/

    /*
    public popn(cnt: number): SECDArray{
        let result: SECDArray = new SECDArray();
        for(let i = 0; i < cnt; i ++){
            result.push(this.pop());
        }
        return result;
    }*/
/*
    public popArray(): SECDArray{
        let x = this.pop()
        if(Array.isArray(x))
            return x
        return null
    }*/

}